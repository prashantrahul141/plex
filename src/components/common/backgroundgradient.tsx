import type { FC } from 'react';

const BackgroundGradient: FC = () => {
  return (
    <div className='background-gradient fixed top-0 left-0 -z-50 h-screen w-screen'></div>
  );
};

export default BackgroundGradient;
